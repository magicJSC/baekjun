#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
#include <cmath>
#include <stack>
using namespace std;

int main() {
	long long a, b;
	while (cin >> a >> b)
	{
		cout << b / (a + 1) << "\n";
	}
	return 0;
}
