#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
#include <cmath>
#include <deque>
using namespace std;

#define fastIo ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);


int main() {
	int a, b, c, d, e;
	cin >> a >> b >> c >> d >> e;
	cout << a * 6 + b * 3 + c * 2 + d + e * 2<<" ";
	cin >> a >> b >> c >> d >> e;
	cout << a * 6 + b * 3 + c * 2 + d + e * 2;

	return 0;
}
